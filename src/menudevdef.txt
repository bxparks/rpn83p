# Same as menudef.txt, but reduced down so that the assembly code data can be
# hand coded during development.

MenuGroup root mRoot [
  MenuStrip [
    MenuGroup NUM mNum [
      MenuStrip [
        MenuItem <Scaret>3 mCube
        MenuItem CBRT mCubeRoot # maybe use <ScubeR><Sroot>
        MenuItem ATN2 mAtan2
        MenuItem <Spercent> mPercent
        MenuItem * *
      ]
      MenuStrip [ 
        MenuItem ABS mAbs
        MenuItem SIGN mSign
        MenuItem MOD mMod
        MenuItem LCM mLcm
        MenuItem GCD mGcd
      ]
    ]
    MenuGroup PROB mProb [
      MenuStrip [
        MenuItem PERM mPerm
        MenuItem COMB mComb
        MenuItem N<Sexclam> mFactorial
        MenuItem RAND mRandom
        MenuItem SEED mRandomSeed
      ]
    ]
    MenuItem * * # CONV
    MenuItem * * # BASE
    MenuItem HELP mHelp
  ]

  MenuStrip [
    MenuGroup MODE mMode [
      MenuStrip [
        MenuItem RAD mRad
        MenuItem DEG mDeg
      MenuItem * *
      MenuItem * *
      MenuItem * *
      ]
    ]
    MenuItem DISP mDisp
    MenuItem HYP mHyperbolic
    MenuItem UNIT mUnit
    MenuItem * *
  ]
]
