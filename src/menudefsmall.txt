# Same as menudef.txt, but reduced down to a proof of concept so that the
# data tables can be hand coded for development.

MenuGroup root mRoot [
  MenuStrip [
    MenuGroup NUM mNum [
      MenuStrip [
        MenuItem ^3 mCube
        MenuItem CBRT mCubeRoot # maybe use <super3><root> ?
        MenuItem ATN2 mAtan2
        MenuItem % mPercent
        MenuItem * *
      ]
      MenuStrip [ 
        MenuItem ABS mAbs
        MenuItem SIGN mSign
        MenuItem MOD mMod
        MenuItem LCM mLcm
        MenuItem GCD mGcd
      ]
    ]
    MenuGroup PROB mProb [
      MenuStrip [
        MenuItem PERM mPermutation
        MenuItem COMB mCombination
        MenuItem N! mFactorial
        MenuItem RND mRandom
        MenuItem SEED mRandomSeed
      ]
    ]
    MenuItem * * # CONV
    MenuItem * * # BASE
    MenuItem HELP mHelp
  ]
]
